// On sépare en 2 datasets
const budgetOk = budgetCumul.map((val, i) => (val <= budgetMaxValue ? val : null));
const budgetDepasse = budgetCumul.map((val, i) => (val > budgetMaxValue ? val : null));

datasets: [
  {
    label: 'Budget cumul OK',
    data: budgetOk,
    type: 'line',
    borderColor: 'green',
    stepped: true,
    fill: false
  },
  {
    label: 'Budget dépassé',
    data: budgetDepasse,
    type: 'line',
    borderColor: 'red',
    stepped: true,
    fill: false
  }
]
