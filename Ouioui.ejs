{
  label: 'Budget cumul',
  data: budgetCumul,
  type: 'line',
  stepped: true,
  borderColor: (ctx) => {
    const i = ctx.p0DataIndex;   // point de départ du segment
    const val = ctx.p0.parsed.y; // valeur au début du segment
    if (val > budgetMaxValue) {
      return 'red';
    }
    return 'green';
  },
  fill: false
}
